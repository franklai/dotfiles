# Dotfiles Configuration

## Installation

```bash
sh -c "$(curl -fsLS get.chezmoi.io)" -- init --apply franklai
```

## Environment

* Vim config
* `oh-my-zsh`
* nvm
* Homebrew
* diff-so-fancy, fd, ag, rg
